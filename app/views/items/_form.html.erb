<%= form_for [current_user, item] do |f| %>
	<%= f.label :name %>
	<%= f.text_area :name, rows:8, class: "form-control", placeholder: "Enter To Do list item" %>
	<%= f.submit "Save", class: "btn btn-info" %>
<% end %>

